<!DOCTYPE html>
<html>
  <head>
    <% if params[:nav] == "bot" %>
      <title>Arena Wars Bot</title>
      <meta name="description" content="Langeweile in Discord? Dann laden Sie doch einfach buhrmis Arenakampf-Bot in Ihren Server ein.">
    <% else %>
      <title>Arena Wars</title>
      <meta name="description" content="The reimagination of a classic 2004 browser-based game.">
    <% end %>
    
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= vite_client_tag %>
    <%= vite_javascript_tag 'application' %>
   
    <%= inertia_ssr_head %>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
